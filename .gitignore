# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# data folder
data/*
!data/.gitkeep
!data/photos

# photos folder in data
data/photos/*
!data/photos/.gitkeep
